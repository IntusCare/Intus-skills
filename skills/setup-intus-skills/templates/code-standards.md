<!-- SEED: strip every SEED comment. Every bullet must be traceable to a written rule in this repo.
     If the repo has no written standards, do not write this file — skip it and say so. -->
# Code Standards That Bite During Implementation

> **TL;DR** — The full list with rationale is `<the authority>`. These are the ones that bite
> mid-implementation.

## Context

`implement` and `fix` ship in the shared `intus-skills` plugin and cannot carry a repo's standards.
This is the short list `<repo>` wants in front of an agent while it writes code, not the authority —
where this file and `<the authority>` disagree, the authority wins. `fix` also reads it as the list
of rules a review finding can never talk it into breaking: a suggestion that needs one of these
violated gets declined, with the rule cited.

## The list

<!-- SEED: up to ~8 bullets. Each one states the rule, and the consequence when it's broken where
     that isn't obvious. Point at the full rule in parentheses. Order by how often an agent trips
     on it, not by importance. Drop anything an agent would get right anyway. -->

- `<rule>` — `<what breaks when it's violated>` (`<pointer to the full rule>`)
- `<rule>`
